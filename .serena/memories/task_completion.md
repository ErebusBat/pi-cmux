# Task completion

- TypeScript-affecting changes: run `npm run typecheck` and require zero diagnostics.
- Package publication/content changes: run `npm run pack:check` in addition to the relevant focused verification.