# Core

- npm package `pi-cmux`; distributable source lives in `extensions/`; package entry config is in `package.json`.
- Public/package documentation is in README.md, CHANGELOG.md, and docs/.
- Build/tooling details: `mem:tech_stack`; routine commands: `mem:suggested_commands`; completion checks: `mem:task_completion`; source conventions: `mem:conventions`.